templates:

groups:
  hub1:
    role: hub
    cfengine:
      version: 3.27.foo

    source:
      mode: spawn
      spawn:
        provider: vagrant
        vagrant:
          box: ubuntu/focal64

      count: 1
